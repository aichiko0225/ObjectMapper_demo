source 'https://github.com/CocoaPods/Specs.git'
source 'https://git.oschina.net/rarexray/LLPaySDKSpecs.git'

target 'ObjectMapper_demo' do
platform :ios, '8.0'
use_frameworks!

pod 'ObjectMapper', '~> 3.3'

    target 'ObjectMapper_demoTests' do
        inherit! :search_paths
    end
end
